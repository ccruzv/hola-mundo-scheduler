apiVersion: v1
kind: ResourceQuota
metadata:
  name: scheduler-quota
  namespace: schedulers
spec:
  hard:
    requests.cpu: "200m"
    requests.memory: "256Mi"
    limits.cpu: "500m"
    limits.memory: "512Mi"
    pods: "5"
